# `ko-main-b1200-m5-s0` 중단 조사 (2026-09-25)

## 중단 원인과 진행 범위

- Modal app ID: `ap-7qjCkn9Pq6tK2CVRSlrf4y`; 세션 ID: `49bbbf7aa0fe485aaab1fdbb5e80aacc`.
- `run_status.json`의 최종 상태는 `interrupted`, 오류는 `APITimeoutError`다. Modal 로그의 스택 추적은 GEPA 29번째 반복에서 후보 평가용 **문제풀이(task) API 호출** 중 `httpx.ReadTimeout`이 발생했음을 가리킨다. 해당 호출은 HTTP 응답을 받지 못했으므로 제공자 서버와 네트워크 중 어느 쪽이 지연 원인인지는 판정할 수 없다.
- API 요청 기록의 마지막 호출은 논리적 호출 442번이다. 120초 제한과 SDK 재시도 2회를 거쳐 약 361.5초 뒤 실패했다. 전체 기록은 task 성공 429건·실패 1건, reflection 성공 9건·시간 초과 3건이다. 앞선 reflection 시간 초과 세 건과 최종 중단 원인은 서로 다른 단계에 있다.
- `run_log.json`에는 **완료된 반복 28회**가 있다. 29번째 반복 도중 중단했다. 저장된 GEPA 상태는 metric call 425/1200, 전체 validation 4회다. 사용하지 못한 잔여 예산이 있다는 뜻이며, 본실험의 정상 완료 결과는 아니다.

## 중단 시점까지의 validation

| 후보 | 생성 반복 | validation | 해석 |
| --- | ---: | ---: | --- |
| 초기 후보 0 | 0 | 46/60 (76.7%) | 기준선 |
| 후보 1 | 7 | **58/60 (96.7%)** | 저장된 최고점 |
| 후보 2 | 9 | 54/60 (90.0%) | 점수 하락 |
| 후보 3 | 26 | 54/60 (90.0%) | 점수 하락 |

후보 1은 마지막 줄에 `정답: 숫자`를 반드시 쓰고 숫자에 단위·기호를 붙이지 않도록 명시했으며, 풀이 단계 설명과 검산도 추가했다. 후보 2는 문제 조건, 단위 변환, 할인 및 합계 해석을 강조했다. 후보 3은 마지막 줄의 형식을 더 엄격히 했다. 28회 반복 중 신규 후보 3개가 채택되었지만 최고 validation 점수는 7번째 반복 이후 갱신되지 않았다. 29번째 반복의 제안은 평가 도중 멈췄으므로 후보·점수를 확정할 수 없다.

이 validation은 기존 HRM8K/GSM8K 기반 60문항과 기존 형식 중심 채점기를 따른다. 58/60은 수학적 추론 능력 향상의 독립적 증거가 아니다. 후보를 이 점수로 선택했으므로 같은 validation 점수를 다시 일반화 성능으로 해석해서도 안 된다. 별도 test 평가와 풀이 오류 분석이 필요하다.

## 확인 근거

원격 Volume `motif-gepa-ko-runs/ko-main-b1200-m5-s0`에서 `run_status.json`, `api_requests.jsonl`, `run_log.json`, `gepa_state.json`, `validation.jsonl`, `candidates.json`, `events.jsonl`를 `runs/remote_snapshots/`에 내려받아 확인했다. 이 원자료와 API 요청·응답은 Git에서 제외된다. 원본 프롬프트 전체는 Volume의 `candidates.json`에 보존되어 있다.

## 다음 실행의 시간 설정

이번 실행의 Motif 클라이언트에는 요청당 120초 제한과 SDK 재시도 2회가 적용됐고, Modal 함수 자체의 제한은 86,400초였다. 따라서 이번 `APITimeoutError`는 Modal 함수 시간 제한 때문에 발생한 것이 아니다. 2026-09-25에 향후 실행의 Motif 요청 제한을 1,800초로 늘리고 어려운 수학 문제의 출력 한도를 16,384토큰으로 설정했다. 긴 제한도 제공자·게이트웨이 자체의 제한이나 장애를 없앨 수는 없다. 재시도 끝에도 응답이 없으면 기록을 남기고 체크포인트에서 재개해야 하며, 응답이 없는 문항을 수학 오답으로 처리해서는 안 된다.
